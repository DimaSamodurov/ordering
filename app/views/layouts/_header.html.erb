<h1><%= link_to "Замовлення Хлібу", root_path %></h1>
<% if user_signed_in? %>
    <%= link_to current_user.email, edit_user_registration_path %>
    <%= link_to "Logout", destroy_user_session_path, method: :delete %>
<% end %>
</div>
<div class="col-xs-6 text-right">
  <h1 class="cart-text"> <%= render 'layouts/cart_text' if !current_order&.order_items&.blank? %></h1>
</div>
</div>
